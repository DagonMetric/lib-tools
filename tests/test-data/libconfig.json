{
  "$schema": "../../dist/schemas/schema.json",
  "projects": {
    "hello-project": {
      "root": ".",
      "tasks": {
        "hello": {
          "handler": "./tasks.mjs"
        },
        "echo": {
          "handler": "exec: echo \"Hello exec!\""
        },
        "build": {
          "handler": "exec: echo \"Hello custom build handler\""
        }
      }
    }
  }
}
